import styled from 'styled-components';

// & + div toda div que tem uma div antes dela, a primeira não deve ter;
export const Container = styled.div`
    display : flex;
    padding : 30px;
    height : calc(100% - 80px);
`;
